import React from "react";
import ReactDOM from "react-dom";
import { PinquinRadio } from "./PinquinRadio";

const rootElement = document.getElementById("root");
ReactDOM.render(
  <React.StrictMode>
    <PinquinRadio />
  </React.StrictMode>,
  rootElement
);
